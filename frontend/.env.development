# Empty base URL: requests go to /api on the Vite dev server, which proxies to VITE_PROXY_TARGET (no CORS needed).
VITE_API_BASE_URL=
VITE_API_TIMEOUT_MS=30000
VITE_PROXY_TARGET=http://localhost:8080
