# Server
PORT=8080

# Database (either set DATABASE_URL, or the individual DB_* variables)
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/products_db?sslmode=disable
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=products_db
DB_SSLMODE=disable
